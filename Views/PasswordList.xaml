<Window x:Class="PasswordManager.Views.PasswordList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager.Views"
        mc:Ignorable="d"
        Title="Портативный менеджер паролей(список)" Height="862" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#3D3D3D">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопка "Назад" -->
        <Button x:Name="BackButton"
                Grid.Row="0"
                Content="Назад"
                FontSize="14"
                Padding="15,8"
                Cursor="Hand"
                Click="BackButton_Click"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,0,0,20"
                Background="#1976D2"
                Foreground="Black"
                BorderBrush="Black"
                BorderThickness="2">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#1E88E5"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#0D47A1"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <!-- Заголовок -->
        <Border Grid.Row="0"
                Background="#2E7D32"
                BorderBrush="Black"
                BorderThickness="2"
                CornerRadius="10"
                Padding="20"
                Margin="0,40,0,30"
                HorizontalAlignment="Center"
                Width="400">
            <TextBlock Text="Список паролей"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="Black"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Уведомление о копировании (скрыто по умолчанию) -->
        <TextBlock x:Name="CopyNotification"
                   Grid.Row="0"
                   Text="Скопировано!"
                   FontSize="14"
                   Foreground="#4CAF50"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Top"
                   Margin="0,50,20,0"
                   Visibility="Collapsed"/>

        <!-- Список карточек -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="CredentialsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#E0E0E0"
                                BorderBrush="Black"
                                BorderThickness="2"
                                CornerRadius="10"
                                Padding="20"
                                Margin="0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Текст: сервис, логин, пароль -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               Margin="0,0,0,5">
                                        <Run Text="Название сервиса: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding ServiceName}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="14"
                                               Foreground="#333333"
                                               Margin="0,2">
                                        <Run Text="Логин: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding Login}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="14"
                                               Foreground="#333333"
                                               Margin="0,2">
                                        <Run Text="Пароль: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding Password}"/>
                                    </TextBlock>
                                
                                    <!-- Кнопка "Удалить" -->
                                    <Button FontSize="12"
                                            Padding="10,5"
                                            Cursor="Hand"
                                            Click="DeleteButton_Click"
                                            Tag="{Binding Id}"
                                            Background="#D32F2F"
                                            Foreground="White"
                                            BorderBrush="Black"
                                            BorderThickness="2"
                                            Margin="0,5">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <StackPanel Orientation="Horizontal"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center">
                                                                    <Image Source="/PasswordManager;component/icons/trash.png"
                                                                        Width="16" Height="16"
                                                                        Margin="0,0,5,0"/>
                                                                    <TextBlock Text="Удалить"
                                                                            Foreground="{TemplateBinding Foreground}"
                                                                            VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#E53935"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#B71C1C"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Кнопка "Скопировать логин" -->
                                    <Button FontSize="12"
                                            Padding="10,5"
                                            Cursor="Hand"
                                            Click="CopyLoginButton_Click"
                                            Tag="{Binding Login}"
                                            Background="#1976D2"
                                            Foreground="Black"
                                            BorderBrush="Black"
                                            BorderThickness="2"
                                            Margin="0,5">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <StackPanel Orientation="Horizontal"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center">
                                                                    <Image Source="/PasswordManager;component/icons/copy.png"
                                                                        Width="16" Height="16"
                                                                        Margin="0,0,5,0"/>
                                                                    <TextBlock Text="Скопировать логин"
                                                                            Foreground="{TemplateBinding Foreground}"
                                                                            VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#1E88E5"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#0D47A1"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Кнопка "Скопировать пароль" с иконкой -->
                                    <Button FontSize="12"
                                            Padding="10,5"
                                            Cursor="Hand"
                                            Click="CopyPasswordButton_Click"
                                            Tag="{Binding Password}"
                                            Background="#1976D2"
                                            Foreground="Black"
                                            BorderBrush="Black"
                                            BorderThickness="2"
                                            Margin="0,5">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="8"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <StackPanel Orientation="Horizontal"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center">
                                                                    <Image Source="/PasswordManager;component/icons/copy.png"
                                                                        Width="16" Height="16"
                                                                        Margin="0,0,5,0"/>
                                                                    <TextBlock Text="Скопировать пароль"
                                                                            Foreground="{TemplateBinding Foreground}"
                                                                            VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                                </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#1E88E5"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#0D47A1"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>